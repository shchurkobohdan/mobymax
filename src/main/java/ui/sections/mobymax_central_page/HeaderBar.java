package ui.sections.mobymax_central_page;

import core.element.Element;
import org.openqa.selenium.WebElement;

public class HeaderBar extends Element {

	public HeaderBar(WebElement webElement) {
		super(webElement);
	}
}
